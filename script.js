function add (a, b){
    return a+b;
};

function subtract (a, b){
    return a-b;
};

function multiply (a, b) {
    return a*b;
};

function divide (a, b) {
    return a / b;
};

const buttons = document.querySelectorAll('.button');

console.log(buttons)
// buttons.addEventListener('click', () => {
//     if (buttons.classList.contains('plus'))

// })